function deleteComment(commentsId, commentIdex) {
	var comments = document.getElementById(commentsId);
	comments.removeChild(comments.childNodes[commentIndex]);
}

function addComment() {
	var comments = document.getElementById(commentsId);
	/* Element aufbauen und anhängen */
}
